public class Main{
    public static void main(String[] args) {
        //LinkedList<Person> people = new LinkedList<>();

        // Person p = new Person(21, "Simran");
        
        // people.insert(p);
        // people.insert(new Person(18, "Sonal"));
        // people.insert(new Person(28, "Shriya"));

        // people.traverse();
        // people.remove(p);
        // people.traverse();

        // for finding the middle element
        // people.insert(new Person(18, "Sonal"));
        // people.insert(new Person(28, "Shriya"));
        // people.insert(new Person(28, "Ria"));
        // people.insert(new Person(28, "Diya"));
        // people.insert(new Person(28, "Dhrithi"));

        // System.out.println("Middle item is :"+people.getMiddleNode());

        // Reverse a linked list inputs
        LinkedList<Integer> list = new LinkedList<>();

        list.insert(12);
        list.insert(30);
        list.insert(78);
        list.insert(90);
        list.insert(5);

        list.reverseList();
    }
}